---
const { url, frontmatter} = Astro.props
---
<div class="m-5 p-5 rounded-md grid grid-rows-[auto,1fr,auto] border-black border-4 text-center shadow-black shadow-lg bg-white hover:bg-slate-200">
    <a href={url}>
        <img class="mx-auto" width="100" height="100"src={frontmatter.image.url} alt={frontmatter.image.alt}>
        <h1 class="text-xl mb-5">{frontmatter.title}</h1>
        <p class="text-sm text-gray-600 self-end">{frontmatter.description}</p>
    </a>
</div>
